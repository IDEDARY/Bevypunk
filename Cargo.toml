[package]
name = "bevy-lunex-cyberpunk"
version = "0.1.0"
edition = "2021"

[profile.dev]
opt-level = 1

[profile.dev.package."*"]
opt-level = 3


[dependencies]
ahash = "0.8.3"
#Currently requires extra features, so I had to fork Bevy. But PR is on its way, hopefully it will arrive in 0.12 or on main in few days
bevy = { git = "https://github.com/idedary/bevy", features = ["dynamic_linking"] }
rand = "0.8.5"

#bevy_lunex = { git = "https://github.com/bytestring-net/bevy-lunex", branch = "main" }
bevy_lunex = { path = "/home/dominik/Projects/Bytestring/bevy_lunex" }
